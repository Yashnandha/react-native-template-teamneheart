import React from 'react';
import { View, Text } from 'react-native';

const Welcome = (props) => {
     return (
          <View>
               <Text>index</Text>
          </View>
     );
};

export default Welcome;
